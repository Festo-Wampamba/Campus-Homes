import { Skeleton } from "@/components/ui/skeleton";

// Shown during slow server navigations within the landlord portal (e.g. right
// after submitting a property on a weak connection) so the route swaps a
// skeleton in rather than a blank white screen while the RSC payload loads.
export default function LandlordLoading() {
  return (
    <div className="space-y-6" aria-busy="true" aria-label="Loading">
      <div className="space-y-2">
        <Skeleton className="h-8 w-56" />
        <Skeleton className="h-4 w-72" />
      </div>
      <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
        {Array.from({ length: 4 }).map((_, i) => (
          <Skeleton key={i} className="h-24 w-full" />
        ))}
      </div>
      <Skeleton className="h-64 w-full" />
    </div>
  );
}
