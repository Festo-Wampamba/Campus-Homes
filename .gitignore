# deps
node_modules/

# env — secrets never committed
.env
.env.*
!.env.example

# build output
dist/
.next/
*.tsbuildinfo

# test
coverage/

# misc
.DS_Store
*.log

# local resources (design references, surveys — not source code)
OwnResourceFolder/

# generated tool output (graphify knowledge-graph artifacts)
graphify-out/

# playwright MCP session artifacts (console logs, page snapshots, screenshots)
.playwright-mcp/

# subagent-driven-development scratch state (progress ledger, task briefs)
.superpowers/

# DB dumps — real user data, never committed
db-backups/

# local git worktrees
.worktrees/
.claude/worktrees/

# Claude Code local machine settings (personal permission overrides)
.claude/settings.local.json
.gstack/
